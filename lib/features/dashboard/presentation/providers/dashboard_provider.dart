// TODO: Dashboard provider — fetch summary stats for the current user's role
// Admin sees: total enterprises, coaches, sessions, institutions
// Supervisor sees: enterprises under their coaches, session counts
// Coach sees: assigned enterprises, upcoming sessions, pending tasks

import 'package:flutter_riverpod/flutter_riverpod.dart';

// TODO: final dashboardProvider = FutureProvider<DashboardStats>((ref) async { ... });
