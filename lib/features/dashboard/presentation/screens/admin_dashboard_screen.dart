// TODO: Admin dashboard — shown to program_admin and institution_admin roles
// Key stats: total enterprises, active coaches, session count, institutions
// Use StatCard widgets in a grid layout

import 'package:flutter/material.dart';

class AdminDashboardScreen extends StatelessWidget {
  const AdminDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Admin Dashboard — TODO')),
    );
  }
}
