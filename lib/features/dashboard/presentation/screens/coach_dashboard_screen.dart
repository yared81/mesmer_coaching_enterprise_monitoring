// TODO: Coach dashboard — primary screen for coach role
// Key stats: my enterprises count, sessions this month, pending tasks
// Quick actions: Register Enterprise, Start Session, View Schedule

import 'package:flutter/material.dart';

class CoachDashboardScreen extends StatelessWidget {
  const CoachDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Coach Dashboard — TODO')),
    );
  }
}
