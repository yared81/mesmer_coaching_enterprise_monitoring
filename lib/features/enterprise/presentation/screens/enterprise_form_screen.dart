// TODO: Enterprise registration form — multi-step form for coach to register a new enterprise
// Step 1: Business name, sector, employee count
// Step 2: Owner name, phone, email, location
// Step 3: Baseline business information (financial, operational)
// Submit: POST /enterprises

import 'package:flutter/material.dart';

class EnterpriseFormScreen extends StatelessWidget {
  const EnterpriseFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Enterprise Registration Form — TODO')),
    );
  }
}
