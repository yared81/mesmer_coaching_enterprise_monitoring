// TODO: Enterprise list screen — shows all enterprises assigned to current coach
// Features: search bar, filter by sector, pull-to-refresh
// Each item: EnterpriseCard widget
// FAB: navigate to EnterpriseFormScreen to register new enterprise

import 'package:flutter/material.dart';

class EnterpriseListScreen extends StatelessWidget {
  const EnterpriseListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Enterprise List — TODO')),
    );
  }
}
