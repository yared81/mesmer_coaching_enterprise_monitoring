// TODO: Supervisor analytics dashboard screen
// Sections:
//   - Header stats: total enterprises, coaches, sessions, avg improvement
//   - Bar chart: enterprises by sector (fl_chart BarChart)
//   - Line chart: sessions over time (fl_chart LineChart)
//   - Table: top performing enterprises

import 'package:flutter/material.dart';

class SupervisorReportsScreen extends StatelessWidget {
  const SupervisorReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Supervisor Reports — TODO')),
    );
  }
}
