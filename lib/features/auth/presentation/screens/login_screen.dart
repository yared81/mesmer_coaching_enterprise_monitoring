// TODO: Login screen
// - Email + password fields (use AppTextField)
// - Login button (use PrimaryButton with loading state)
// - Forgot password link → navigates to ForgotPasswordScreen
// - On success: GoRouter redirects based on user role

import 'package:flutter/material.dart';

class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement login form UI
    return const Scaffold(
      body: Center(child: Text('Login Screen — TODO')),
    );
  }
}
